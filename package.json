{
  "name": "cocobet-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "CocoBet Dashboard — Frontend-Render-Tests (jsdom).",
  "scripts": {
    "test": "node --test tests/frontend/*.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^24.1.3"
  }
}
