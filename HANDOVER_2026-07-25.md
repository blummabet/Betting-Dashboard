# Handover — Session 25.07.2026

Kontext für die nächste Session. Die Memory-Dateien laden ohnehin automatisch — das hier ist die Kurzfassung + Push-Liste.

## Was diese Session gebaut/geändert wurde

**1. Sieg-/Ungeschlagen-Serie → 1X2/DC** (Lucas: „5 Siege in Folge sollten die 1X2 beeinflussen")
- `fetch_wm_form.py`: `wonSeq`/`unbeatenSeq` + `winRate`/`unbeatenRate`; `wonSeq` neuer Schema-Stale-Trigger.
- `compute_streaks.py`: win/unbeaten-Serien + Gegner-Faktoren.
- `sharp_signals/streak_momentum.py`: neuer `_eval_result`-Pfad (gebackte Seite +, Gegner −, pmul win 0.4/unbeaten 0.45).
- Feuert erst, nachdem der nächste Runner die Form neu geholt hat (wonSeq via Schema-Stale).

**2. MLS Kontext-0/3-Gap gefixt** (Anreiz/Kontext)
- Root-Cause war NICHT fehlende Standings (die existieren, `league_pressure` liest sie).
- `conviction_score.py`: `league_pressure` + `mls_travel` in die Kontext-Familie aufgenommen. 3 MLS-Heim-Picks bekommen jetzt Kontext-Punkt (einer → Conviction 7).

**3. Conviction-Familien SSOT-Refactor**
- `conviction_score.py`: 4 inline-Listen → ein Dict `CONVICTION_FAMILIES` + `CONTEXT_UNCREDITED`.
- Bewusst NICHT aus registry.SIGNAL_GROUPS abgeleitet (andere Taxonomie).
- Neuer Guard-Test `TestFamilyRegistrySync`: neues Kontext-Signal kann nicht mehr still aus der Wertung fallen.

**4. Backtest der 4 ungewerteten Kontext-Signale**
- Ergebnis: KEINE aufnehmen. fixture_congestion 0.502/n=287 (Null), game_state_openness 0.550/n=40 (zu dünn), coach_change/transfer_shift keine Daten.

**5. `league_pressure` Richtungs-Bug gefixt**
- Backtest: Heimsieg 0.479 / Auswärtssieg 0.444 (beide <0.5). Sieg-Boost für „Punkte-brauchendes" Team ist anti-prädiktiv (Jagd-Team = schwächer).
- `team_pressure` gibt jetzt 3-Tupel `(pressure, motive, branch)`. `evaluate` boostet nur aus `title/hold/releg`, nicht mehr aus `chase`.
- `game_state_openness.py` Aufrufer mit angepasst (3-Tupel).
- Feuert aktuell 0× (greift ~Runde 20), validiert sich beim nächsten `liga_backtest.py`-Lauf.

## Push-Liste (via GitHub Desktop — alles noch uncommitted)
- `fetch_wm_form.py`
- `compute_streaks.py`
- `sharp_signals/streak_momentum.py`
- `sharp_signals/league_pressure.py`
- `sharp_signals/game_state_openness.py`
- `conviction_score.py`
- `tests/test_streak_momentum.py`
- `tests/test_form_refetch.py`
- `tests/test_conviction_score.py`
- `tests/test_league_pressure.py`
- `tests/test_mls_league_pressure.py`

Status: **1814 Tests grün**, berührte Datendateien revertiert.

## Offene Follow-ups
- Nach dem nächsten `liga_backtest.py`-Lauf (API): prüfen, ob league_pressure|Heimsieg/Auswärtssieg über 0.5 steigt → bestätigt den Richtungs-Fix.
- `game_state_openness` bei mehr Auflösungen erneut backtesten (0.550 aber nur n=40).
- Optional: Kontext-Familie später doch aus registry ableiten — nur nach Backtest der 4 CONTEXT_UNCREDITED-Signale.
